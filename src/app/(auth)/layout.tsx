/**
 * AuthLayout — Layout transparente para el grupo (auth).
 * LoginPage gestiona su propio centrado y estructura de dos columnas.
 */
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
